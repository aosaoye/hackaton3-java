import java.util.Scanner;
import java.util.regex.Pattern;

public class Ej15 {
    static boolean comprobar_si_existe(char[] palabra, char character) {

        for() {
            if (palabra[i] == character) return true
        }

        return false;
    }
    public static void execute() {
        Scanner sc = new Scanner(System.in);
        int vida = 5;
        char[] palabra = {'J','A', 'V', 'A'};
        char[] progreso = {'_', '_', '_', '_'};

        while(vida > 0) {
            // input
            boolean has_input = sc.hasNext(Pattern.compile("."));
            char letra = sc.next().charAt(0);
            
            for() {
                input == letras[i]
            }
            }
            if (comprobar_si_existe()) {
                --vida;
            }

            _,_,_,
        }
    }
}
